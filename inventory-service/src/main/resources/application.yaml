spring:
  datasource:
    url: jdbc:mysql://localhost:3306/inventory_db?createDatabaseIfNotExist=true
    username: root
    password: 123456

  application:
    name: inventory-service

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 8081